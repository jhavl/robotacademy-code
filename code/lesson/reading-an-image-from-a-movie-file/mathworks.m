%% 1:18
cam = VideoReader('traffic_sequence.mp4')

%% 1:31
videoFrame = readFrame(cam);
imshow(videoFrame)

%% 1:37
videoFrame = readFrame(cam);
imshow(videoFrame)

%% 1:38
videoFrame = readFrame(cam);
imshow(videoFrame)

%% 1:40
videoFrame = readFrame(cam);
imshow(videoFrame)

%% 1:41
videoFrame = readFrame(cam);
imshow(videoFrame)

%% 1:42
videoFrame = readFrame(cam);
imshow(videoFrame)

%% 1:50
cam